<a
        href="{{ link }}"
        target="{{ target }}"
        style="
                display: inline-block;
                padding: 12px 20px;
                background: {{ bgColor }};
                color: {{ textColor }};
                border-radius: 6px;
                text-decoration: none;
                cursor: pointer;
                "
>
    {{ text }}
</a>
